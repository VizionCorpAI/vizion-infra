# Secrets and Identity

## Principles
- No secrets in repos
- Use environment + secrets manager
- Rotate any secret pasted into chat

## Identity
- Systemd services and containers use scoped secrets
- Workspace-specific credentials are scoped to that workspace
